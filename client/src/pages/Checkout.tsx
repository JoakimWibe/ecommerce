import { Heading } from "@chakra-ui/react"

const Checkout = () => {
    return (
        <Heading as={"h3"}>Only logged in users should be able to see this!</Heading>
    )
}

export default Checkout